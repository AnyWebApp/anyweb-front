import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  listItem: {
    color: 'grey'
  },
  listItemIcon: {
    minWidth: '24px',
    paddingRight: '15px'

  }
});
