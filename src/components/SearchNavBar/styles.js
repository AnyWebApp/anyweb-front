import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  tabsBox: {
    display: 'flex',
    width: '60%'
  },
  menuIcon: {
    padding: 0,
    minWidth: 50
  }
});

